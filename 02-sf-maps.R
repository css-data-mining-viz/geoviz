library(tidyverse)
library(tidycensus)

# Obtain information on median household income in 2017 for Cook County, IL
# at the tract-level using the ACS. To retrieve the geographic features for
# each tract, set `geometry = TRUE` in your function.
# 
# HINT: median household income is called B19013_001 by the Census Bureau


# Draw a choropleth using the median household income data.
# Use a continuous color gradient to identify each tract's median household income.


# Draw the same choropleth for Cook County, but convert median household income
# into a discrete variable with 6 levels.


# Draw the same choropleth for Cook County using the discrete variable,
# but select an appropriate color palette using Color Brewer


# Use the `viridis` color palette for the Cook County map drawn
# using the continuous measure.


